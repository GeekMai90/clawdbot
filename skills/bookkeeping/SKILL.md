---
name: bookkeeping
description: 记账技能（Obsidian Markdown 存储）：记账 / 报销 / 订阅提醒。使用场景：(1) 记录日常收入支出并按月统计 (2) 管理公司报销事项和待报销清单 (3) 订阅服务管理与续费提醒（提前3天/1天/当天）
---

# 记账技能（bookkeeping）🧾

> 数据存储位置：Obsidian Vault `GeekMaiOB/记账/`（Markdown）。

## 模块划分（重要）

### 1) 记账模块
- 记录你的**收入/支出**（类似记账软件）
- **按月存储**：每月一个文件 `YYYY-MM.md`
- 支出带分类（内置常用分类，后续可增减）

### 2) 报销模块
- 记录公司采购等需要你先垫付的项目
- 需要时输出"报销单纯文字"给你
- 你说"已报销完"后：**清空待报销列表**

### 3) 订阅模块
- 记录每月/每年的软件订阅
- 续费提醒：**提前3天、提前1天、当天**
- 续费后更新下次续费日期

---

## 数据文件（Obsidian 内）

目录：`GeekMaiOB/记账/`

- `YYYY-MM.md`：月度账单
- `待报销.md`：待报销清单（报销完删除/清空）
- `订阅管理.md`：订阅清单

---

## 命令

### 记账模块

- 记录收入：
  ```bash
  node scripts/bookkeeping.js income <金额> <备注...>
  ```

- 记录支出：
  ```bash
  node scripts/bookkeeping.js expense <金额> <分类> <备注...>
  ```

- 查看月度账单：
  ```bash
  node scripts/bookkeeping.js monthly [YYYY-MM]
  ```

- 月度统计：
  ```bash
  node scripts/bookkeeping.js stats [YYYY-MM]
  ```

**内置分类（可调整）**：餐饮、交通、购物、娱乐、医疗、教育、住房、通讯、数码、其他

---

### 报销模块

- 添加待报销：
  ```bash
  node scripts/bookkeeping.js reimburse <金额> <事由...>
  ```

- 查看/整理待报销清单（纯文字）：
  ```bash
  node scripts/bookkeeping.js reimburse-list
  ```

- 清空待报销：
  ```bash
  node scripts/bookkeeping.js reimburse-clear
  ```

---

### 订阅模块

- 添加订阅：
  ```bash
  node scripts/bookkeeping.js subscribe <名称> <金额表达> <周期(月/年)> <下次续费日期YYYY-MM-DD>
  ```

- 查看订阅：
  ```bash
  node scripts/bookkeeping.js subscribe-list
  ```

- 续费后更新下次续费日期：
  ```bash
  node scripts/bookkeeping.js subscribe-renew <名称> <新日期YYYY-MM-DD>
  ```

- 删除订阅：
  ```bash
  node scripts/bookkeeping.js subscribe-remove <名称>
  ```

- 检查提醒（用于 cron 每日检查）：
  ```bash
  node scripts/bookkeeping.js subscribe-check
  ```

---

## 备份功能

- 备份当前数据到 `记账/_backup/YYYY-MM-DD/`：
  ```bash
  node scripts/bookkeeping.js backup
  ```
