# 模式切换约定（给技能作者/维护者看的）

## 默认：召回
用户表达包含：
- “搜Get”
- “查Get”
- “Get里找”
- “去Get笔记里搜”

→ 一律走 recall。

## 显式：问答
用户表达包含关键词：
- “问答Get”
- “问答Get笔记”
- 句子里出现“问答”且语义明确是让 Get 回答

→ 走 qa。

## 显式：只召回不总结
用户表达包含：
- “只召回”
- “不要总结/不要回答/只给片段/只给原文”

→ recall + 结果原样输出。
