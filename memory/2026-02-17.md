
## 2026-02-17 14:40 Discord 接入与排障进展
- 麦先生决定同时保留 Telegram + Discord，开始搭建 Discord 作为多频道工作流入口。
- 已完成：
  - 创建 Discord 服务器：`麦先生的后花园`
  - 创建并邀请 Bot（灵瑶）入服
  - 给 Bot 提升了较高权限（含管理频道）
  - 创建了 `日常` 分类及 `闲聊`、`内观` 频道
  - 我已成功写入两个频道的 topic（闲聊=普通助手模式；内观=更锋利的反思模式）
- OpenClaw 侧已完成：
  - Discord channel 配置接入并可连接（状态 ON/OK）
  - OpenClaw 从 `2026.2.13` 更新到 `2026.2.15`
- 当前核心问题（未完全解决）：
  - Bot 可在 Discord 频道发送消息（发送正常）
  - 但用户在 `#闲聊` 的多条消息未稳定触发自动回复（接收/事件路由异常）
  - 日志曾出现 `reason: no-mention`，后续又出现“可读到消息历史但未触发入站处理”的现象
- 已尝试过的配置方向：
  - `groupPolicy: open` 与 `allowlist` 两种模式
  - guild 级 `requireMention: false`
  - 显式 channel allowlist（闲聊/内观/常规）
  - 多次重启 Gateway 并监控日志
- 用户反馈偏好：
  - 很在意“执行重启/改配置后要及时告知”
  - 希望一步步、可见进度地排查，不要无提示中断
