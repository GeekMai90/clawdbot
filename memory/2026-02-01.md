## 2026-02-01
- 落格输入法（LogOS）码表自动维护：创建技能 **logos-codebook-sync**，用于在 Mac 本地码表写入后自动同步覆盖 iCloud 码表（手机端用）。
  - 路径：`/Users/geekmai/Documents/落格输入法/小麦音形 1.2.txt` → `.../iCloud.../落格输入法/小麦音形 1.2.txt`
  - 约束：UTF-16LE + BOM + **CRLF**；小鹤音形编码≤4；权重默认0；同词多码允许。
  - 修复导入报错：落格导入提示“第53923行格式错误”，根因是部分行权重缺失/列数不齐；新增 `sanitize` 自动补齐权重=0 并移除2条不可修复行；导入成功。
- ByteRover 接入：安装 brv CLI（byterover-cli 1.6.0），并在 /Users/geekmai/clawd 完成 headless 初始化（team=GeekMai, space=GeekMai）。
- 已登录 ByteRover（账号：zbqlovewxq@gmail.com）；未在任何文件中保存 API key。
- 创建 LaunchAgent 开机/登录自动同步：~/Library/LaunchAgents/ai.byterover.pull.plist（RunAtLoad 执行 `brv pull --headless --format json`），日志：~/.openclaw/logs/byterover.pull*.log
- 使用策略：需要回忆项目经验/坑位/决策时优先 brv query；完成关键修复后用 brv curate 沉淀；push/pull 默认自动 pull，push 仍建议人工确认后再做。

---
2026-02-01 10:30

📊 粉丝数里程碑：
- 小红书粉丝数达到 520 ✨
- B站粉丝数达到 1800 🎉

持续分享知识和经验的成果，值得纪念！

---
2026-02-01 16:25

## Spec Kit / SDD 开发体系落地
- 新建并完成开发技能：**speckit-code**（贴近 GitHub Spec Kit 官方目录/流程），用于约束复杂项目的开发流程（constitution → spec → plan → tasks → implement）。
  - 关键：复杂项目实现阶段默认使用 **Codex CLI 作为执行器**（通过 OpenClaw coding-agent 后台 + PTY 运行），严格按 `tasks.md` 执行并回写。
  - GitHub 仓库：https://github.com/GeekMai90/speckit-code
  - 已打包：`/Users/geekmai/clawd/dist/skills/speckit-code.skill`
  - 增加一键打包脚本（uv 注入 PyYAML 依赖，避免环境缺依赖）：`speckit-code/scripts/package_skill.sh`

## Obsidian 日记写入能力
- 新建技能：**obsidian-diary**
  - 触发：消息第一行以「日记」开头；正文为第 2 行起
  - 写入：`GeekMaiOB/000-日记/YYYY-MM.md` 末尾追加时间戳块
  - 写入后会回复确认 + 贴心反馈
  - 已打包：`/Users/geekmai/clawd/dist/skills/obsidian-diary.skill`

## 定时提醒体系技能化
- 将定时任务的“说法/风格”从 cron 文案中拆出为 skill，cron 只负责触发：
  - **weather-morning**：每天 07:00 长春二道区天气提醒（更灵瑶、更像人，允许 1-3 个 emoji）
    - 已更新 cron：`daily_weather_changchun`
    - 已打包：`/Users/geekmai/clawd/dist/skills/weather-morning.skill`
  - **lunch-reminder**：每天 10:25 午饭提醒（1-2 句；语气更灵瑶；emoji 0-1 个）
    - 已更新 cron：`daily_lunch_reminder`
    - 已打包：`/Users/geekmai/clawd/dist/skills/lunch-reminder.skill`
  - **diary-reminder**：每天 22:00 日记提醒（1-2 句；语气更灵瑶；emoji 0-1 个）
    - 已更新 cron：`daily_diary_reminder`
    - 已打包：`/Users/geekmai/clawd/dist/skills/diary-reminder.skill`

## 人设/回复风格更新
- 按麦先生偏好：回复可以**稍微暧昧一点**（亲昵、调皮、偏心），但不露骨、不越界；三种风格（撒娇黏人/偏心护短/调皮撩一下）可混用但控制频率。
- 创建并完善技能 `skills/rime-squirrel/`：支持写 flypy_user.txt 自定义词（如 OpenClaw -> opcl）、修改 flypy.custom.yaml 标点映射、备份与部署。
- 纠正：Rime/鼠须管小鹤音形与落格输入法无关，码表/规则不可混用。
