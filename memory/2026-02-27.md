# 2026-02-27 日志

## Obsidian 每日笔记自动整理（obsidian_daily_note_polish）

- Cron ID: `b2ba0f75-8bd3-414f-909f-17fb486008e8`
- 运行时间：每天 23:50 Asia/Shanghai
- 功能：读取当天每日笔记 → 整理为标准格式 → 生成「今日最佳」AI 高亮 → 自动记账检测
- 格式要求：
  - `## 📝 日记清单` 包含原始条目
  - `## 🌟 今日最佳` 只有 1 条，最多 2 句，不加粗，不用固定开头模板
  - 三个分析维度：①行为镜像 ②跨界联想 ③潜台词挖掘
- 自动记账：检测日记条目中的明确金额+消费行为，查重后调用 `node bookkeeping.js expense`
- 静默运行：不发 Telegram 通知（麦先生 23:50 已睡）
- 已格式化检测：检查是否含 `## 📝 日记清单`，有则跳过

## 取消日记提醒

- 已删除 22:00 日记提醒 cron（ID: `f0206198-9373-42ec-b39c-220f8aa9d3f7`）
- 原因：麦先生不需要了

## Claude Pro 退订

- 已在 `GeekMaiOB/30-运行记录/财务记录/订阅管理.md` 标记 Claude Pro 为已退订（2026-02-26）

## Star-Office-UI NAS 部署

- 目标：在 NAS（192.168.99.150）Docker 运行像素办公室状态显示 Web App
- 仓库来源：https://github.com/ringhyacinth/Star-Office-UI.git
- NAS 路径：`/volume1/docker/star-office-ui/`
- 端口：Host 13011 → Container 18791
- 关键修复：
  - `backend/app.py` 原 `ROOT_DIR` 硬编码为 `/root/.openclaw/workspace/star-office-ui`，已改为 `os.environ.get("APP_ROOT", "/app")`
  - `requirements.txt` 内容：`flask`
  - `state.json` 复制自 `state.sample.json`
  - 使用自定义 Dockerfile（python:3.11-slim，暴露 18791）
- 进度：Dockerfile 已创建，待构建 Docker 镜像并运行容器
- 下一步：
  ```bash
  docker build -t star-office-ui /volume1/docker/star-office-ui/
  docker run -d --name star-office-ui -p 13011:18791 \
    -v /volume1/docker/star-office-ui/state.json:/app/state.json \
    --restart unless-stopped star-office-ui
  curl http://192.168.99.150:13011/health
  ```
- SSH 登录：host `192.168.99.150`, port `22`, user `GeekMai`, 密码存在脑子里（用 expect，因为 sshpass 不可用）

## daily_micro_distill 问题

- 当天无 `memory/YYYY-MM-DD.md` 时报错（文件不存在），不影响核心功能
- 需要在脚本中加 graceful exit guard（如果文件不存在则静默退出）

## 技术决策

- 不安装 memory-lancedb-pro：qmd 已覆盖混合检索，LanceDB 黑盒不如 Markdown 透明可备份
- Star-Office-UI：装饰性像素办公室状态 UI，目前暂无操作
