# 2026-03-03 日志

## 技能与系统变更

- **readwise-translate 技能新建**：用户发链接+"保存"触发，自动抓取→翻译中文（符合中文习惯、忠实原意、末尾附原文链接）→保存到 Readwise Reader。Token 存 Keychain `readwise_token`，标签：`译文`、`灵瑶翻译`，位置：`new`。脚本：`skills/readwise-translate/scripts/save_to_readwise.sh`。
- **flashnote-capsule-sync 技能更新**：移除 dinox 文件夹，现在只监听 voicenotes。`SOURCE_DIRS` 已改为仅 `voicenotes`，`byFolder` 统计也去掉了 dinox 字段。
- **bookkeeping-monthly-report 路径修复**：`monthly_report.py` 中 `BOOKKEEPING_DIR` 从旧路径 `02-生活/记账` 改为新路径 `30-运行记录/财务记录`，与主脚本 bookkeeping.js 保持一致。
- **OpenClaw 更新**：`2026.3.1` → `2026.3.2`，新版本支持 Telegram 流式输出。
- **Telegram 流式输出已开启**：`blockStreamingDefault: "off"`，边生成边发送，重启后生效。

## 用户行为与偏好

- **Readwise 翻译规范**：符合中文语言习惯（不生硬）、保留原作者原意、文章末尾附原文英文链接。不需要保留英文原文正文（太长影响阅读），只附链接即可。
- **Readwise Token**：`47XdovBQltCQ2SNOEAxJlebSmtHfQHO4anCaqLQ8BWYkEmbIMk`，存 Keychain `readwise_token`。
- **订阅记录**：2026-03-02 订阅了 Claude Pro 和 ChatGPT Plus，各 $20/月，下次续费日 2026-04-02，已在 Apple Reminders 设置 3 月 30 日取消提醒。

## 踩坑记录

- **daily_note_polish 写文件报错**：路径含 `~` 导致 Write 工具拒绝写入，需要用完整绝对路径或 exec+python3 写文件。
- **sendMessageDraft**：Telegram Bot API 9.3 新接口，OpenClaw 2026.3.2 已支持，通过 `blockStreamingDefault: "off"` 开启。之前版本未实现。
- **config.patch 无法删除 key**：patch 只能合并，删除 provider/字段必须用 python3 直接操作 JSON 文件再 restart。
